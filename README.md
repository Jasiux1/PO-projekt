# PO-projekt

Prosty serwis Flask do przechowywania użytkowników.

## Endpointy
- `GET /users`
- `GET /users/<id>`
- `POST /users`
- `PATCH /users/<id>`
- `DELETE /users/<id>`

## Uruchomienie testów
```bash
py -3.12 -m pytest -q
```
